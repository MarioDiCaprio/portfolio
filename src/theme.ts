import {createTheme} from "@mui/material";


export const theme = createTheme({
    palette: {
        primary: {
            main: '#64ffda'
        },
        text: {
            primary: '#ccd6f6',
            secondary: '#8892b0'
        },
        background: {
            default: '#0a192f'
        }
    }
});
